node{
     stage('SCM checkout'){
    git 'https://github.com/javahometech/my-app.git'
    }
    stage('compile-package'){
    //Geting maven home path
    def mvnhome = tool name: 'maven 3', type: 'maven'
    sh "${mvnhome}/bin/mvn package"
    }
    }
